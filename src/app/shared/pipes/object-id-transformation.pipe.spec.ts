import {ObjectIdTransformationPipe} from './object-id-transformation.pipe';

describe('ObjectIdTransformationPipe', () => {
  it('should be defined', () => {
    expect(new ObjectIdTransformationPipe()).toBeDefined();
  });
});
