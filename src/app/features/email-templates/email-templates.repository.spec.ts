import {EmailTemplatesRepository} from './email-templates.repository';

describe('EmailTemplatesRepository', () => {
  it('should be defined', () => {
    expect(new EmailTemplatesRepository()).toBeDefined();
  });
});
